package playlist

import "net/http"

func (service *Service) HandleGetPlaylist(
	w http.ResponseWriter, r *http.Request,
) {
	// get default playlist and all collectibles in it
}

func (service *Service) HandlePostPlaylist(
	w http.ResponseWriter, r *http.Request,
) {
	// get default playlist and all collectibles in it
}

func (service *Service) HandlePutPlaylist(
	w http.ResponseWriter, r *http.Request,
) {
	// get default playlist and all collectibles in it
}
